package LinkedLists;

public interface SingleLinkedListFunctions {

	String traversal(SingleListNode head);

	String topK(SingleListNode head, int k);

	String minK(SingleListNode head, int k);

	String reverseTraversal(SingleListNode head);

}
